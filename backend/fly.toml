# fly.toml app configuration file generated for ielts-grader-backend on 2026-06-04T21:14:04+05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'ielts-grader-backend'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile'

[env]
  NODE_ENV = 'production'
  PORT = '8080'
  PYTHONUNBUFFERED = '1'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

# Persist Social Ops discovery CSVs / briefs across deploys (one volume ↔ one machine)
[[mounts]]
  source = 'social_ops_data'
  destination = '/app/social-media/output'

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
